name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.ref || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: "0"
  CARGO_NET_RETRY: 10
  RUST_BACKTRACE: 1

# Komutlar bilerek sade: .githooks/pre-push bu dosyadaki `run: cargo …`
# satirlarini okuyup aynen calistirir. Buraya bir kontrol eklemek, yerel
# kapiya da eklemek demektir — iki listeyi el ile esitlemek gerekmez.
#
# NEDEN IKI CHECKOUT: ortak Turkce katlama kardes depoda (`katla = { path =
# "../katla" }`, 2026-09-26). actions/checkout workspace disina yazamaz; bu
# yuzden depo bir alt dizine (el-Fihrist/) cikarilir, katla yanina konur —
# pasli-beyin ci.yml ile ayni kalip. Yerel kapi etkilenmez, orada yol var.
jobs:
  test:
    name: Test (${{ matrix.os }})
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: el-Fihrist
    steps:
      - uses: actions/checkout@v4
        with:
          path: el-Fihrist
      - name: Checkout katla (path bagimliligi)
        uses: actions/checkout@v4
        with:
          repository: Ercaner1988/katla
          path: katla
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          prefix-key: "v1-rust"
          workspaces: "el-Fihrist -> target"
          cache-on-failure: true
      - name: Build
        run: cargo build --workspace --all-targets
      - name: Run tests
        run: cargo test --workspace

  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: el-Fihrist
    steps:
      - uses: actions/checkout@v4
        with:
          path: el-Fihrist
      - name: Checkout katla (path bagimliligi)
        uses: actions/checkout@v4
        with:
          repository: Ercaner1988/katla
          path: katla
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
        with:
          prefix-key: "v1-rust-lint"
          workspaces: "el-Fihrist -> target"
          cache-on-failure: true
      - name: Check formatting
        run: cargo fmt --all -- --check
      # `--all-features` BİLEREK YOK: `onnx` feature'ı `fastembed` → `ort`
      # ağacını çeker ve derleme sırasında ONNX Runtime ikilisini İNDİRİR.
      # Her push'ta yüz megabaytlarca indirme, varsayılan derlemede hiç
      # kullanılmayan bir çekirdek için. ONNX bacağı yerelde denetlenir:
      #   cargo clippy --features onnx --all-targets -- -D warnings
      - name: Run clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
